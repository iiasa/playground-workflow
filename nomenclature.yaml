repositories:
  common-definitions:
    url: https://github.com/IAMconsortium/common-definitions.git/
definitions:
  region:
    repository:
      name: common-definitions
      include:
        - hierarchy: common
        - hierarchy: R5
        - hierarchy: R9
        - hierarchy: R10
    country: true
